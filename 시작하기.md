# Claude Code에서 이어서 작업하기

## 1. 폴더 준비

압축을 풀어 원하는 위치에 두고, 그 폴더에서 터미널을 엽니다.

```bash
cd ~/projects/atec-reservation    # 압축 푼 폴더 경로
git init
git add .
git commit -m "ATEC교육장 예약시스템 초기 버전"
```

`git init`은 필수는 아니지만, 해 두면 Claude Code가 고친 내용을 되돌리거나 비교하기 쉬워집니다.

## 2. Claude Code 실행

```bash
claude
```

같은 폴더에 있는 `CLAUDE.md`를 Claude Code가 자동으로 읽습니다. 여기에 지금까지의 설계 결정과 주의사항이 정리되어 있어서, 처음부터 설명하지 않아도 맥락을 이어받습니다.

## 3. 첫 지시 예시

이어서 할 일에 따라 이렇게 시작하시면 됩니다.

**로컬에서 화면부터 확인하고 싶을 때**

```
CLAUDE.md의 로컬 미리보기 방법대로 미리보기를 띄워줘.
```

**주의사항 문구를 채울 때**

```
사용 주의사항 문구를 아래 내용으로 채워줘. AGREEMENT_VERSION도 같이 올려줘.
1. (문구)
2. (문구)
...
```

**Firebase를 연결할 때**

```
Firebase 설정값을 아래로 교체해줘.
apiKey: ...
authDomain: ...
projectId: ...
storageBucket: ...
messagingSenderId: ...
appId: ...
```

**UI를 고칠 때**

```
담당자 화면의 예약 카드에 사용 목적을 접었다 펼 수 있게 해줘.
고친 뒤에는 미리보기로 렌더링과 콘솔 오류를 확인해줘.
```

---

## 4. 남은 작업 순서 (권장)

1. **Firebase 프로젝트 생성** — 콘솔에서 새 프로젝트를 만듭니다. 치유농업센터용 `reservation-ahd`와 반드시 별도로 만들어야 데이터와 담당자 권한이 섞이지 않습니다.
   - Firestore Database 생성 (위치: `asia-northeast3`)
   - Authentication > Sign-in method > **Google** 사용 설정
   - 프로젝트 설정 > 내 앱 > 웹 앱 추가 → 나오는 설정값을 `index.html`의 `firebaseConfig`에 붙여넣기

2. **담당자 등록** — Firestore에 `admins` 컬렉션을 만들고, 문서 ID를 담당자 구글 이메일로 하는 빈 문서를 추가합니다. (예: `dksemffl90@gmail.com`)

3. **보안규칙 적용** — `firestore.rules` 내용을 Firestore Database > 규칙 에 붙여넣고 게시합니다.

4. **사용 주의사항 문구 작성** — `AGREEMENT_ITEMS`와 `AGREEMENT_VERSION`을 함께 채웁니다.

5. **GitHub Pages 배포**
   ```bash
   gh repo create Gyeongnam-ares/Reservation-system-ATEC --public --source=. --push
   ```
   저장소 Settings > Pages 에서 브랜치를 지정해 배포합니다.

6. **승인된 도메인 추가** — Firebase 콘솔 Authentication > Settings > 승인된 도메인에 `gyeongnam-ares.github.io`를 추가합니다. **이 단계를 빠뜨리면 배포 후 구글 로그인이 실패합니다.**

7. **실사용 전 점검** — 실제 신청 → 동의 → 승인 → CSV 내보내기까지 한 번 돌려보고, 테스트로 만든 예약은 담당자 화면에서 삭제합니다.

---

## 5. 확인이 필요한 사항

- ATEC 영상교육장과 세미나실의 **운영시간이 서로 같은지**. 현재는 두 곳 모두 평일 09:00~18:00으로 되어 있습니다. 다르면 구조를 조금 바꿔야 합니다.
- 공휴일 목록은 2026~2027년치만 들어 있습니다. **2028년 이후는 매년 갱신**해야 합니다.
